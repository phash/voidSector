# Task Completion Checklist

1. Verify TypeScript compiles: `npx tsc --noEmit` in the relevant package
2. Run tests if applicable: `npm test` in the relevant package
3. Stage and commit with descriptive message
4. Use `feat:` prefix for new features, `fix:` for bug fixes
